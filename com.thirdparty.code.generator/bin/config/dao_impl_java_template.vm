package ${package_name};

#foreach ($import_class in $import_classes)
$import_class
#end

@Repository
public class ${class_name} implements ${implements_interfaces} {

#foreach ($field in $fields)
$field
#end

#foreach ($method in $methods)
$method
#end

}